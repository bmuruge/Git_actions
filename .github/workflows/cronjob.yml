name: Cronjob
on: 
  schedule:
    - corn: '* * * * *'


jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
    - name: Clone repo
      uses: actions/checkout@v4

    - name: Setup python
    - uses: actions/setup-python@v5
      with:
        python-version: '3.13' 
   
    - name: Run python script
      uses: python hello1.py